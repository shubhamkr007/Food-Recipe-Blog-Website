<div class="row flex-lg-row-reverse align-items-center g-5 py-4 mb-4">
    <div class="col-12 col-lg-6">
        <img src="/img/hero-image.png" width="607" height="510" class="d-block mx-lg-auto img-fluid" loading="lazy" alt="Cooking Blog">
    </div>

    <div class="col-12 col-lg-6">
        <h1 class="display-5 fw-bold mb-3">Huge selection of delicios recipe ideas</h1>
        <p class="lead">
            Explore our huge selection of delicious recipe ideas including; easy desserts, delicious vegan and vegetarian dinner ideas, gorgeous pasta recipes, quick bakes, family-friendly meals and gluten-free recipes.
        </p>
        
        <div class="d-grid gap-2 d-md-flex justfiy-content-md-start">
            <a href="/explore-latest" class="btn btn-primary btn-dark btn-lg px-4 ml-3 mr-3">Explore Latest</a>
            <a href="/explore-random" class="btn btn-outline-secondary btn-lg px-4 me-md-2">Show Random</a>
        </div>
    </div>
</div>

<!-- categories -->

<div class="row row-cols-2 row-cols-lg-6 g-2 g-lg-3 py-4">

    <% if(categories!= ''){ %>
        <% categories.forEach((category, index)=>{ %>
            <a href="/categories/<%- category.name %>" class="col text-center category__link ">
                <div class="category__img shadow">
                    <img src="img/<%- category.image %>" alt="<%- category.name %>" loading="lazy">
                </div>
                <div class="pt-1"><%- category.name %></div>
            </a>
        <% }) %>
    <% } %>

    <a href="/categories" class="col text-center category__link">
        <div class="category__img shadow">
          <img src="img/view-all.jpg" alt="View All Categories" loading="lazy">
        </div>
        <div class="pt-1">View All</div>
    </a>

</div>

<!-- Latest -->

<section class="pb-4 pt-4 ">
    <div class="d-flex mb-2 align-items-center">
        <h2>Latest Recipes</h2>
        <a href="/explore-latest" class="ml-auto"> View More</a>
    </div>
    <div class="row row-cols-2 row-cols-lg-5 g-5 g2 g-lg-3">

        <% if(food!=''){ %>
            <% food.latest.forEach((recipe, index)=>{ %>
                <a href="/recipe/<%= recipe._id %>" class="col text-center category__link">
                    <div class="category__img category__img--large shadow">
                        <img src="uploads/<%= recipe.image %>" alt="<%= recipe.name %>>" loading="lazy">
                    </div>
                    <div class="pt-1"><%= recipe.name %></div>    
                </a>
            <% }) %>
        <% } %>

    </div>
</section>

<!-- Thai -->

<section class="pb-4 pt-4 ">
    <div class="d-flex mb-2 align-items-center">
        <h2>Thai Recipes</h2>
        <a href="/categories/Thai" class="ml-auto">View More</a>
    </div>

    <div class="row row-cols-2 row-cols-lg-5 g-5 g2 g-lg-3">

        <% if(food!=''){ %>
            <% food.thai.forEach((recipe, index)=>{ %>
                <a href="/recipe/<%= recipe._id %>" class="col text-center category__link">
                    <div class="category__img category__img--large shadow">
                        <img src="uploads/<%= recipe.image %>" alt="<%= recipe.name %>>" loading="lazy">
                    </div>
                    <div class="pt-1"><%= recipe.name %></div>    
                </a>
            <% }) %>
        <% } %>

    </div>
</section>

<!-- American -->

<section class="pb-4 pt-4 ">
    <div class="d-flex mb-2 align-items-center">
        <h2>American Recipes</h2>
        <a href="/categories/American" class="ml-auto">View More</a>
    </div>

    <div class="row row-cols-2 row-cols-lg-5 g-5 g2 g-lg-3">

        <% if(food!=''){ %>
            <% food.american.forEach((recipe, index)=>{ %>
                <a href="/recipe/<%= recipe._id %>" class="col text-center category__link">
                    <div class="category__img category__img--large shadow">
                        <img src="uploads/<%= recipe.image %>" alt="<%= recipe.name %>>" loading="lazy">
                    </div>
                    <div class="pt-1"><%= recipe.name %></div>    
                </a>
            <% }) %>
        <% } %>

    </div>
</section>




<!-- Submit -->

<section class="px-4 py-5 my-5 text-center">
    <img src="/img/publish-recipe.png" class="d-block mx-auto mb-4 img-fluid" alt="Publish Recipe" width="556" height="208" loading="lazy">
    <h1 class="display-5 fw-bold">Publish Your Recipe</h1>
    <div class="col-lg-6 mx-auto">
        <p class="lead mb-4">Publish your Recipes in front of thousand of people for FREE</p>
        <div d-grid gap-2 d-sm-flex justify-content-sm-center>
            <a href="/submit-recipe" class="btn btn-orimary btn-dark btn-lg">Submit Recipe</a>
        </div>
    </div>
</section>